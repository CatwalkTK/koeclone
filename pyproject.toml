[project]
name = "koeclone"
version = "0.1.0"
description = "ローカル音声クローン・テキスト読み上げシステム（単一ユーザー・日本語）"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.116",
    "uvicorn>=0.35",
]

[dependency-groups]
dev = [
    "pytest>=8.4",
    "httpx>=0.28",
    "ruff>=0.12",
]
